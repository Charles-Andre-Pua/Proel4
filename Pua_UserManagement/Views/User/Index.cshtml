@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model List<Person>

<div class="container">
    <div class="text-center">
        <table class="table">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Name</th>
                    <th>Email</th>
                    <th>Username</th>
                    <th>Password</th>
                    <th>Date Created</th>
                    <th>Date Updated</th>
                    <th><a href="/User/Create" class="btn btn-primary">Create</a></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var person in Model)
                {
                    <tr>
                        <td>@person.User.Id</td>
                        <td>@person.Lastname,@person.Firstname</td>
                        <td>@person.Email</td>
                        <td>@person.User.Username</td>
                        <td>@person.User.Password</td>
                        <td>@person.DateCreated</td>
                        <td>@(person.DateUpdated is null ? "-" : person.DateUpdated)</td>
                        <td>
                            <a href="#!" data-id="@person.Id" class="editPerson btn btn-primary" data-bs-toggle="modal" data-bs-target="#updateModal">Edit</a>
                            <a href="#!" data-id="@person.Id" class="delPerson btn btn-secondary" data-bs-toggle="modal" data-bs-target="#delModal">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

<!-- Update Modal -->
<div class="modal fade" id="updateModal" tabindex="-1" aria-labelledby="editModal" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h1 class="modal-title fs-5" id="exampleModalLabel">Modal title</h1>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                ...
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                <button type="button" class="btn btn-primary">Save changes</button>
            </div>
        </div>
    </div>
</div>

<!-- Delete Modal -->
<div class="modal fade" id="delModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h1 class="modal-title fs-5" id="exampleModalLabel">Modal title</h1>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                ...
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                <button type="button" class="btn btn-primary">Save changes</button>
            </div>
        </div>
    </div>
</div>


@section Scripts {
    <script>
        $('.edt').click(function(){
            let id = $(this).data("id");
            #('#editModal').load('/User/UpdatePartial/${id}')
        });
    </script>
}